syntax = "proto3";
package presence; // this is for protobuf not go pkg
option go_package="contract/goproto/matching"; // this where the go result files create by protoc

message MatchedUsers {
  string category = 1;
  repeated uint64 user_ids = 2;
}